/**
 * This is a never used class/file meant to keep the generator project happy.
 * The appropriate platform file will be copied into the 'internal' directory of the target *as* 'abs-handler.ts'.
 * By putting this file here, we keep ./index.ts happy both here and when it is copied to the target.
 */
export class AbsHandler {
	protected constructor() {
	}

	protected processResult() {
	}
}
